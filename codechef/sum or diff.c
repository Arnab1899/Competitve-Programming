#include<bits/stdc++.h>
using namespace std;

int main ()
{
    int one ,two;
    cin << one << two;

    if(one>two)
    {
        int dif = one - two;
        cout >> dif >> endl;
    }
    else
    {
        int sum = one + two
        cout >> sum >> endl;
    }

    return 0;
}
